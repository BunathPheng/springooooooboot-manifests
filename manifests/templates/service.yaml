---
# Source: helm for kubernetes/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: springooooooboot          # Service name
  labels:
    app: springooooooboot
spec:
  type: ClusterIP   # Service type (ClusterIP, NodePort, LoadBalancer)
  selector:
    app: springooooooboot         # Matches Deployment labels
  ports:
    - protocol: TCP
      port: 8080         # Service port
      targetPort: 8080  # Target container port
